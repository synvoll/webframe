# example resulting type textfile for checkin

# Paketname
package=value filled into html form by user

# Version
version=value filled into html form by user

# Verteilart
delivery=Clientpaket (if checked by user)

# Kurzbeschreibung
comment=text written into html form by user

# Installation (like checked by user in html form)
unifa=true
bap=false
basic=true
other=false